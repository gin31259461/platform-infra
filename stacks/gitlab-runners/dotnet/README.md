# .NET Runner stack

Copy `config.example.yml` to `config.yml`, then set the GitLab endpoint,
network interface, resource limits, and image allowlists. Project paths,
NuGet feeds, SDK versions, runtime versions, and SQL Server versions remain in
the consuming project's `.gitlab-ci.yml`.
