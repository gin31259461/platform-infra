---
# Copy this file to config.yml. Replace every REPLACE_* value before use.
stack:
  type: gitlab-runner
  id: dotnet
  description: .NET GitLab Runner

gitlab:
  url: https://REPLACE_WITH_GITLAB_HOSTNAME
  hostname: REPLACE_WITH_GITLAB_HOSTNAME
  health_url: https://REPLACE_WITH_GITLAB_HOSTNAME/users/sign_in

runner:
  name: dotnet-podman
  user: gitlab-runner-dotnet
  container_name: gitlab-runner-dotnet
  service_name: gitlab-runner-dotnet

  # Confirm compatibility with the target GitLab version before upgrading.
  image: docker.io/gitlab/gitlab-runner:v18.10.1

  tags:
    - dotnet
    - podman

  concurrent: 1
  cpus: 4
  memory: 6g
  shm_size_bytes: 1073741824
  pull_policy: if-not-present
  privileged: false
  default_job_image: mcr.microsoft.com/dotnet/sdk:10.0.302-noble

  allowed_images:
    - mcr.microsoft.com/dotnet/sdk:*
    - mcr.microsoft.com/dotnet/runtime:*
    - mcr.microsoft.com/dotnet/aspnet:*

  allowed_services:
    - mcr.microsoft.com/mssql/server:*

network:
  vpn_interface: REPLACE_WITH_VPN_INTERFACE
  validation_image: docker.io/curlimages/curl:8.12.1
  # Set this to the VPN resolver IP when Host DNS changes during VPN startup.
  vpn_dns: ""
  use_host_network_for_runner_manager: true

tls:
  private_ca_enabled: false
  private_ca_source: ""
