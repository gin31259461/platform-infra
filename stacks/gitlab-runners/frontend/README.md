# Frontend Runner stack

Copy `config.example.yml` to `config.yml`, then set the GitLab endpoint,
network interface, private registry allowlist, resource limits, and optional
private CA path. Project-specific Node, pnpm, and browser versions remain in
the consuming project's `.gitlab-ci.yml`.
