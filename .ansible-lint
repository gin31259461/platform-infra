---
profile: production
exclude_paths:
  - .ansible/
  - .venv/
  - stacks/**/config.yml
skip_list:
  # Roles intentionally share one validated runner_* runtime namespace.
  - var-naming[no-role-prefix]
  # Compact role files use descriptive task names without redundant prefixes.
  - name[prefix]
