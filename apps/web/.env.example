# Local development only. Production authentication must use the approved
# organization identity provider; the development stub must not be deployed.
PLATFORM_AUTH_MODE=development-stub
PLATFORM_FLEET_REPOSITORY=fake
PLATFORM_OBSERVATION_INGESTION=disabled
DATABASE_URL=postgresql://runner_platform:replace_me@127.0.0.1:5432/runner_platform
GITLAB_BASE_URL=https://gitlab.example.invalid
